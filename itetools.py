from itertools import*

#順列
list(permutations([]))

#組み合わせ
list(combinations([],n))

#累積和
list(accumulate([]))

#値を個数で区切るやつ
for key, group in groupby([])

#bit全探索
for l in list(itertools.product([0,1], repeat=n))